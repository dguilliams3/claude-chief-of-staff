{
  "$schema": "./mcp-servers.schema.json",
  "_description": "Registry of SSE URLs for MCP servers offered during setup-instance Phase 4. Edit a single entry here when a provider rotates their endpoint rather than hunting down hardcoded URLs in SKILL.md.",
  "servers": {
    "atlassian": {
      "label": "Jira / Confluence (Atlassian)",
      "cliName": "atlassian",
      "sseUrl": "https://mcp.atlassian.com/v1/sse",
      "description": "Project tracking, issues, Confluence pages. OAuth via Atlassian account."
    },
    "fireflies": {
      "label": "Fireflies",
      "cliName": "fireflies",
      "sseUrl": "https://mcp.fireflies.ai/sse",
      "description": "Meeting transcripts. OAuth via Fireflies account."
    },
    "ms365": {
      "label": "Microsoft 365 (email, calendar, Teams)",
      "cliName": "ms365",
      "sseUrl": null,
      "manualSetup": "Microsoft does not publish a hosted MCP endpoint at a stable public URL as of 2026-04. To connect MS365, install a community MCP server locally (e.g., `microsoft-365-mcp` or similar) and register it with `claude mcp add ms365 -- npx -y <package-name>`. See the MCP registry at https://mcpservers.org or Anthropic's docs for current options.",
      "description": "Outlook mail, calendar events, Teams messages. No hosted MCP; configure a local MCP server during setup."
    }
  }
}
