{
  "name": "claude-chief-of-staff",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "app",
    "server/local",
    "server/worker",
    "agent"
  ],
  "scripts": {
    "start": "concurrently -n api,tunnel -c blue,green \"npx tsx server/local/server.ts\" \"cloudflared tunnel run ${TUNNEL_NAME:-YOUR_TUNNEL_NAME}\"",
    "test": "npm run test --workspace=agent && npm run test --workspace=app && npm run test --workspace=server/worker && npm run test --workspace=server/local && vitest run",
    "typecheck": "tsc --noEmit -p agent/tsconfig.json && tsc --noEmit -p app/tsconfig.app.json && tsc --noEmit -p server/worker/tsconfig.json && tsc --noEmit -p server/local/tsconfig.json",
    "lint:root": "eslint --config eslint.root.config.mjs --max-warnings=0 scripts/check-code-standards.ts scripts/__tests__/check-code-standards.test.ts agent server",
    "lint": "npm run lint:root && npm run lint --workspace=app",
    "check:code-standards": "node --import tsx scripts/check-code-standards.ts",
    "build": "npm run build --workspace=app",
    "preflight": "bash scripts/preflight.sh"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "concurrently": "^10.0.3",
    "eslint": "^10.5.0",
    "globals": "^17.7.0",
    "typescript-eslint": "^8.62.0",
    "vitest": "^4.1.9"
  }
}
