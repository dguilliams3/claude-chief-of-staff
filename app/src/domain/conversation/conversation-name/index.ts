/**
 * ConversationName sub-domain — naming lifecycle for conversations.
 *
 * Upstream: `app/src/store/conversationSlice.ts` — follow-up completion handler
 * See also: `app/src/domain/conversation/` — parent domain
 */

export type { ConversationName } from './types';
export { assignConversationName } from './assignConversationName';
