# Worker API URL — REQUIRED for production builds.
# Set to your deployed Worker URL (e.g., https://cos-api.your-subdomain.workers.dev)
# Build will succeed without this but the app won't be able to reach the API.
VITE_API_URL=
